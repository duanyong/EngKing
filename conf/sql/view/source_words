CREATE VIEW source_words_view AS
    SELECT  UUID() AS id,s.id as source_id, w.id as word_id, ue.user_id, w.word, w.am_mp3, w.am_phonetic, w.en_mp3, w.en_phonetic
    FROM source AS s, english as w, user_english as ue
    WHERE ue.source_id = s.id and ue.english_id = w.id;